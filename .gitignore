node_modules
out
.DS_Store
*.log

# Secrets — nunca commitear
.env
.env.local

# Recursos generados por video (audio/imágenes/música pesadas)
public/media/

# Material de los reels que se editan (video fuente, audio, imágenes descargadas)
public/reel/

# El guión concreto de cada video (se genera; el ejemplo sí se versiona)
input/scenes.json
